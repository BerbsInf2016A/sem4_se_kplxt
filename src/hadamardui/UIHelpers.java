package hadamardui;

import hadamard.Matrix;
import javafx.scene.Node;
import javafx.scene.layout.StackPane;

public class UIHelpers {
    public static StackPane generateMatrix(Matrix changedMatrix) {
        StackPane root = new StackPane();

        return root;
    }
}
